import { AmortizationSchedule } from 'app/preps/model/amortization-schedule';

describe('AmortizationSchedule', () => {
  it('should create an instance', () => {
    expect(new AmortizationSchedule()).toBeTruthy();
  });
});
