import { PrepaymentTimeBalance } from './prepayment-time-balance.model';

describe('PrepaymentTimeBalance', () => {
  it('should create an instance', () => {
    expect(new PrepaymentTimeBalance()).toBeTruthy();
  });
});
